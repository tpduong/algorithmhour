package algorithmhour.sort;

/**
 * Sort
 */
public interface Sort<T> {

    /**
     * sort
     *
     * return a sorted representation of the array.
     * The return object could be the array sorted in place.
     * @param arr
     * @return
     */
    T[] sort(T[] arr);
}
