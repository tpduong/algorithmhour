package algorithmhour.sort.radixsort;

import algorithmhour.sort.Sort;

/**
 * RadixSort
 *
 * implementation of radix sort
 */
public class RadixSort<T extends Comparable<? super T>> implements Sort<T> {

    public T[] sort(T[] arr) {
        return arr;
    }

}
